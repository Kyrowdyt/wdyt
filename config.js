/* ============================================================
   wdyt.live configuration — paste your Supabase values here ONCE.
   Site updates replace index.html but NEVER touch this file,
   so your keys survive every update.

   Where to find the values: Supabase -> Project Settings -> API
   - Project URL        -> SUPABASE_URL
   - anon / public key  -> SUPABASE_ANON_KEY
   (The anon key is public by design. NEVER put the service_role
   key or the database password anywhere in this repository.)
   ============================================================ */
window.WDYT_CONFIG = {
  SUPABASE_URL:      "PASTE_SUPABASE_URL_HERE",
  SUPABASE_ANON_KEY: "PASTE_SUPABASE_ANON_KEY_HERE"
};
